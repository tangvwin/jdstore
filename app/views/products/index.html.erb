

<div class="container">

<div class="row">
  <!-- <% @products.each do |product| %>
    <div class="col-xs-6 col-md-3">
      <%= link_to product_path(product) do %>
        <% if product.image.present? %>
          <%= image_tag(product.image.thumb.url, class: "thumbnail") %>
        <% else %>
          <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
        <% end %>
      <% end %>
            <%= product.title %>
      <% if product.discount.present? %>
        ￥ <%= product.price = product.price * product.discount/100 %>
      <% else %>
        ￥ <%= product.price %>
      <% end %>
    </div>
  <% end %> -->
<% @products.each do |product| %>
  <div class="col-xs-6 col-md-3">
      		<span class="thumbnail">
            <%= link_to product_path(product) do %>
                    <% if product.photos.present? %>
          <%= image_tag(product.photos[0].avatar.thumb.url, class: "thumbnail") %>
      <% else %>
                <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
              <% end %>
            <% end %>
        			<%= product.title %>
              <br>

        			<p> </p>
        			<hr class="line">
        			<div class="row">
        				<div class="col-md-6 col-sm-6">
        					<p class="price"><% if product.discount.present? %>
                    ￥<%= product.price %>
                    <!-- ￥<%= product.price = product.price * product.discount/100 %> -->
                  <% else %>
                     <%= product.price %>
                  <% end %></p>
        				</div>
        				<div class="col-md-6 col-sm-6">
                   <%= link_to product_path(product) do %>
                   <button class="btn btn-info right" > 立即购买</button></a>
                   <% end %>
                   <% if current_user && current_user.is_favorite_of?(@group) %>
       <%= link_to("",add_to_favorite_product_path(product),method: :post, class: "fa fa-heart") %>
     <% else %>
       <%= link_to("", quit_favorite_product_path(product),method: :post,  class: "fa fa-heart", style: "color:red;") %>
     <% end %>

        				 <!-- <a href="http://cookingfoodsworld.blogspot.in/" target="_blank" >	<button class="btn btn-info right" > BUY ITEM</button></a> -->
        				</div>


        			</div>
      		</span>
    		</div>
        <% end %>




</div>

</div>

<!--  分割线   -->











<div class="text-center">
  <%= will_paginate @product, renderer: BootstrapPagination::Rails %>
</div>
