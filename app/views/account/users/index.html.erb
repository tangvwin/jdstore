<!-- <div class="col-md-8 col-md-offset-2">
  <div class="panel panel-info">
    <div class="panel-heading text-center">
      <h3>用户信息</h3>
    </div>
    <div class="panel-footer">
      <table class="table table-boldered">
          <thead>
              <tr>
                  <th>用户名</th>
                  <th>用户头像</th>
                  <th>注册邮箱</th>
              </tr>

          </thead>
          <tbody>




              <% @users.each do |user| %>
                <tr>


                  <td>
                    <% if current_user.name.present? && current_user == user %>

                     <%= link_to(current_user.name, edit_account_user_path(user)) %>

                     <% elsif current_user == user
                        %>
                     <%= link_to(current_user.email, edit_account_user_path(user)) %>

                     <% end %>


                   </td>
                  <td>

                   <% if current_user.profile.present? && current_user == user %>
                      <%= image_tag(current_user.profile.thumb.url,style:"border-radius:50%;width:50px;height:50px;",class:"thumbnail")  %>
                    <% elsif current_user == user%>
                      <%= image_tag("http://ww1.sinaimg.cn/large/006tNbRwgy1ffzxufbo0lj3069069t8o.jpg",class:"thumbnail",style:"width:60px;height:60px;border-radius:50%;") %>
                      <% else %>
                    <% end %>

                  </td>
                  <td>
                    <% if  current_user == user %>


                    <%= current_user.email %>
                    <% else %>

                    <% end %>

                  </td>
                </tr>
              <% end %>
          </tbody>
        </table>
      </div>
    </div>
</div> -->
